  ## Running the code

  'Ctrl + SHift + P' to run rebuild and reopen in containers (dev containers) to down load Node_mudule and some necessory library

  Run `npm run dev` to start the development server.
  